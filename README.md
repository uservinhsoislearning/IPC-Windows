# Inter-process communication
How to run?
Download MinGW, search a video online then follow it. Basically we need to download MinGW, MinGW has a binary file (./bin). You need to include the path to that binary file in your environment variable path.
